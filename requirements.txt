pandas>=2.0.0
scikit-learn>=1.3.0
pydantic>=2.11.0
pyyaml>=6.0

# Internal modules live in pyproject.toml 
# and are installed by the Dockerfile's pinned-version layer.
# Listing them here would break layer caching.
# -----
# stellar-harvest-ie-config>=0.1.2
# stellar-harvest-ie-models>=0.3.1